diff -ru web_scroll_fix_orig/fzastro_ai/web_companion/static/index.html web_scroll_fix_work/fzastro_ai/web_companion/static/index.html
--- web_scroll_fix_orig/fzastro_ai/web_companion/static/index.html	2026-06-16 19:03:59.638243002 +0000
+++ web_scroll_fix_work/fzastro_ai/web_companion/static/index.html	2026-06-16 19:04:02.443921621 +0000
@@ -29,7 +29,7 @@
 
     body {
       margin: 0;
-      overflow: auto;
+      overflow: hidden;
       background:
         radial-gradient(circle at 10% 0%, rgba(91, 151, 218, 0.26), transparent 34rem),
         radial-gradient(circle at 92% 100%, rgba(116, 82, 191, 0.22), transparent 35rem),
@@ -80,6 +80,7 @@
       padding: 0.72rem clamp(0.65rem, 1.5vw, 1rem) 0.85rem;
       height: calc(100dvh - 3.8rem);
       min-height: 0;
+      min-block-size: 0;
       overflow: hidden;
     }
 
@@ -95,7 +96,9 @@
       display: flex;
       flex-direction: column;
       min-height: 0;
+      min-block-size: 0;
       height: 100%;
+      max-height: 100%;
       overflow: hidden;
     }
 
@@ -151,10 +154,12 @@
     }
 
     .messages {
-      flex: 1 1 auto;
+      flex: 1 1 0;
       min-height: 0;
-      height: 100%;
-      overflow-y: auto;
+      min-block-size: 0;
+      height: auto;
+      max-height: none;
+      overflow-y: scroll;
       overflow-x: hidden;
       overscroll-behavior: contain;
       -webkit-overflow-scrolling: touch;
@@ -171,6 +176,7 @@
     .messages::-webkit-scrollbar-thumb, .control-panel::-webkit-scrollbar-thumb { background: rgba(120, 188, 255, 0.18); border-radius: 999px; border: 3px solid rgba(5, 12, 20, 0.85); }
 
     .msg {
+      flex: 0 0 auto;
       border: 1px solid rgba(255, 255, 255, 0.08);
       border-radius: 18px;
       max-width: min(64rem, 94%);
@@ -349,7 +355,7 @@
     }
     .modal-head, .modal-foot { padding: 0.9rem 1rem; border-bottom: 1px solid var(--line); display: flex; justify-content: space-between; align-items: center; gap: 0.7rem; }
     .modal-foot { border-bottom: 0; border-top: 1px solid var(--line); }
-    .modal-body { min-height: 0; overflow: hidden; padding: 1rem; display: grid; grid-template-columns: minmax(0, 1fr) 18rem; gap: 0.85rem; }
+    .modal-body { min-height: 0; overflow: auto; padding: 1rem; display: grid; grid-template-columns: minmax(0, 1fr) 18rem; gap: 0.85rem; }
     .site-map {
       width: 100%;
       height: min(58vh, 34rem);
@@ -423,7 +429,7 @@
       .shell { grid-template-columns: 1fr; height: auto; min-height: calc(100dvh - 3.8rem); overflow: visible; }
       aside.control-panel { order: 2; position: static; max-height: none; overflow: visible; }
       .chat-card { min-height: calc(100dvh - 5.2rem); height: calc(100dvh - 5.2rem); }
-      .messages { min-height: 18rem; }
+      .messages { min-height: 0; flex: 1 1 0; overflow-y: scroll; }
       .subtitle { white-space: normal; }
       .tool-ribbon { overflow-x: auto; }
       .lookup-combo { min-width: 18rem; }
@@ -607,9 +613,17 @@
     let lastSiteResolve = 0;
 
     messagesEl.addEventListener('wheel', event => {
-      // Keep mouse-wheel scrolling inside the chat transcript when possible.
-      if (messagesEl.scrollHeight > messagesEl.clientHeight) event.stopPropagation();
-    }, { passive: true });
+      // Keep mouse-wheel movement inside the transcript. This avoids the page/header
+      // taking the wheel and leaving long tool results visually clipped.
+      if (messagesEl.scrollHeight <= messagesEl.clientHeight) return;
+      const maxTop = messagesEl.scrollHeight - messagesEl.clientHeight;
+      const nextTop = Math.max(0, Math.min(maxTop, messagesEl.scrollTop + event.deltaY));
+      if (nextTop !== messagesEl.scrollTop) {
+        messagesEl.scrollTop = nextTop;
+        event.preventDefault();
+      }
+      event.stopPropagation();
+    }, { passive: false });
 
     tokenInput.value = localStorage.getItem('fzastro_web_token') || '';
     tokenInput.addEventListener('change', () => {
